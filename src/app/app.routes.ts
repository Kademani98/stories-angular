import { Routes } from '@angular/router';
import { StoryBarComponent } from './components/story-bar/story-bar.component';

export const routes: Routes = [
    {
        path: '',
        component: StoryBarComponent
    }
];
